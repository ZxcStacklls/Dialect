💬 Dialect Messenger API (Backend V1.0)Dialect — это гибридная коммуникационная платформа, объединяющая социальные механики, безопасность мессенджеров и инструменты управления сообществами. Данный репозиторий содержит исходный код бэкенда версии 1.0, реализующего базовую архитектуру приложения.🛠 Технический стекПроект построен на современном асинхронном стеке Python:Фреймворк: FastAPI — высокопроизводительный веб-фреймворк для создания API.База данных: MySQL (драйвер pymysql).ORM: SQLAlchemy — работа с базой данных через Python-объекты.Realtime: WebSockets — для мгновенного обмена сообщениями и событиями.Безопасность:JWT (HS256): Авторизация через OAuth2PasswordBearer.Argon2: Современное хеширование паролей.Оптимизация: Bloom Filter (фильтр Блума) — вероятностная структура данных для мгновенной проверки уникальности юзернеймов без лишних запросов к БД.🏛 Архитектура Базы Данных (V1.0)Реализованы следующие основные сущности (app/db/models.py):User (Пользователи)phone_number (Уникальный) — основной идентификатор входа.username (Уникальный) — публичный идентификатор (проверяется через Bloom Filter).public_key — задел для будущего E2EE шифрования.password_hash — хеш пароля (Argon2).Chat (Чаты)Типы: private (ЛС) или group (Группы).Владелец (owner_id) — для управления групповыми чатами.ChatParticipant (Участники)Связывает пользователей и чаты.custom_nickname — локальные никнеймы внутри чата.last_cleared_at — метка времени для функции "Очистить историю для себя".Message (Сообщения)content — хранится в BLOB (подготовка к шифрованию).status — статусы sent, delivered, read.is_pinned — флаг закрепленного сообщения.🚀 Установка и Запуск1. Предварительные требованияPython 3.11+MySQL Server2. Переменные окруженияСоздайте файл .env в корне проекта и укажите настройки подключения (см. app/core/config.py):DB_USER=ваш_юзер_бд
DB_PASSWORD=ваш_пароль_бд
DB_HOST=localhost
DB_PORT=3306
DB_NAME=dialect_db

SECRET_KEY=ваш_секретный_ключ_для_jwt
ACCESS_TOKEN_EXPIRE_MINUTES=60
3. Установка зависимостей pip install -r requirements.txt
(Убедитесь, что установлены fastapi, uvicorn, sqlalchemy, pymysql, python-jose, passlib[argon2], pybloom-live)4. Запуск сервераuvicorn app.main:app --reload
При старте приложение автоматически:Создаст необходимые таблицы в БД (если их нет).Загрузит все существующие юзернеймы в Фильтр Блума (в памяти) для быстрой проверки при регистрации.🌐 API ЭндпоинтыБазовый URL API: /api/v1🔐 Аутентификация (/auth)POST /register — Регистрация нового пользователя (проверяет телефон и username).POST /token — Вход (Login). Принимает username (как телефон) и password, возвращает JWT.👤 Пользователи (/users)GET /me — Получить профиль текущего пользователя.GET /check-username/{username} — Проверка доступности ника (сначала Bloom Filter, затем БД).GET /search — Поиск людей по имени, фамилии, телефону или нику.💬 Чаты (/chats)POST / — Создать ЛС (2 участника) или Группу (до 30 участников).GET / — Список моих чатов.POST /{id}/users — Добавить участника.DELETE /{id}/users/{uid} — Удалить участника / Выйти.PUT /{id}/name — Переименовать группу.DELETE /{id} — Удалить чат (флаг for_everyone для полного удаления).DELETE /{id}/messages — Очистить историю.📨 Сообщения (/messages)GET /history/{chat_id} — История сообщений (учитывает очистку).WS /ws?token=... — WebSocket соединение.События WebSocketСервер принимает и отправляет JSON-события следующих типов:new_message — Отправка/Получение нового сообщения.read — Пометка сообщения прочитанным.edit — Редактирование контента.delete — Удаление сообщения.pin — Закрепление сообщения.🔮 Планы развития (Roadmap V2.0+)Согласно документации, следующие модули запланированы к разработке:Mesh Network: Оффлайн-связь через Bluetooth LE и Wi-Fi Direct.E2EE: Сквозное шифрование для секретных чатов (ключи на клиенте).Discord-like Servers: Полноценные серверы с каналами и ролями.Bot API & Mini Apps: Платформа для разработчиков.Продуктивность: Интеграция календаря и задач.Dialect Team © 2025